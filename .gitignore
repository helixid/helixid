node_modules/
.pnpm-store/
dist/
coverage/
data/
# Any local env file (.env, .env.local, .env-new, .env.whatever). The
# negation keeps the committed template tracked. `*.env` only catches names
# *ending* in .env, which silently missed variants like `.env-new`.
.env*
!.env.example
*.env
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
wallet.enc
__pycache__/
*.pyc
.venv/

# OS and editor files
.DS_Store

# Local-only working material — agent instructions and epic specs.
# Kept on disk for local development, deliberately not published.
CLAUDE.md
specs/
