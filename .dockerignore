.git
.github
node_modules
dist
coverage
.env
.env.local
.env.*.local
*.log
.DS_Store

# NOTE: examples/ and docs/ are deliberately NOT ignored. The root Dockerfile
# never COPYs them, but examples/*/docker/*.Dockerfile build from this repo
# root as their context and do need examples/ present.
